import { BoldTextPipePipe } from './bold-text-pipe.pipe';

describe('BoldTextPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BoldTextPipePipe();
    expect(pipe).toBeTruthy();
  });
});
